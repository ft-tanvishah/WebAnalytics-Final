# Project Guide

This document outlines how to run the project.

## Setup
1. Clone the repository.
2. Install requirements: `pip install -r requirements.txt`
3. Copy `.env.example` to `.env` and configure accordingly.

## Running the app
Run the pipeline using `python run_pipeline.py`.
Run the demo using `python demo.py`.
